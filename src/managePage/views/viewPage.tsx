import { defineComponent } from 'vue';
export default defineComponent({
    setup(props, { emit, slots }) {
        return () => {
            return (
                <span>1113245</span>
            )
        }
    }
})